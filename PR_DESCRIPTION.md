### What

Add a viewers counter feature showing per-platform viewer counts and a total viewers counter in the overlay.

### Changes

- main (Electron main process): aggregate viewers across connectors, broadcast via IPC sp:viewers, expose viewers:get handler, run aggregation every 10s and on channel add/remove.
- preload: expose onViewers/getViewers on window.sp.
- bridge types: add onViewers/getViewers to SpBridge interface.
- UI: add ViewerPanel in ChatPanel (shows only platforms with viewers > 0) and add a footer in OverlayApp showing per-platform icons + counts and a total.

### Notes

- Only platforms with viewers > 0 are shown (as requested).
- Numbers are displayed as integers.
- Safe fallbacks are used if connectors don't expose viewer counts.

### How to test

1. Run the desktop app and open the Channels panel.
2. Add/remove channels and observe the ViewerPanel and Overlay footer update.
3. Verify periodic updates every ~10s.

---

Auto-generated by Copilot.
